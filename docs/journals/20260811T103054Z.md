# Fixed Fastfetch Linux Row Alignment

Corrected the Fastfetch cursor offset for the Linux system name field.

The previous four column offset placed the border over the final character in `Linux`. The field now uses the table width of ten columns.

The focused reproduction changed from `Linu|` to `Linux     |` after the correction.
