# Match cmux to Ghostty

Added an explicit Ghostty compatibility configuration for cmux. The
configuration uses Ghostty's built-in font size, dark background, foreground,
and ANSI color palette. cmux no longer replaces these colors with its adaptive
theme.
