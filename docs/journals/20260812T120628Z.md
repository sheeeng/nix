# Renamed Git Workflow Skills

Renamed the Git workflow skills to use concise imperative names.

1. Renamed the Git commit skill to `commit`.
2. Renamed the GitHub pull request skill to `pull-request`.
3. Renamed the GitHub release skill to `release`.
4. Updated command instructions, OpenCode mappings, and plan references.

The workspace path passed Nix flake evaluation. The repository contains no remaining references to the old prefix.

Added prerequisite checks to the renamed skills. The commit skill now requires a Git work tree. The pull request and release skills also require GitHub to resolve the repository.
