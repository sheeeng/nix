# Carapace Package Collision

Date: 2026-08-31T20:20:00Z

Removed the explicit `carapace` package from the Nushell package list. The
Carapace Home Manager module already provides the wrapped executable through
its Nushell integration, so the explicit package caused a duplicate
`bin/carapace` path in the Home Manager environment.
