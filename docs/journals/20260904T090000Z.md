# Repair Pre-Commit YAML Check

## Date

2026-09-04T09:00:00Z

## Summary

Rebuilt the corrupted pre-commit cache. The `check-yaml` hook had its
`ruamel.yaml` dependency missing from the cached Python environment.

## Verification

`pre-commit run check-yaml --all-files` passes after the cache rebuild.

No repository configuration changes were required.
