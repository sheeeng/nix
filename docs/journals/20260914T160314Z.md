# Add cmux to Development Packages

Added `cmux` to the shared Home Manager development packages for Apple Silicon
macOS hosts. The pinned package supports only `aarch64-darwin`, so Intel macOS
and Linux hosts exclude it.
