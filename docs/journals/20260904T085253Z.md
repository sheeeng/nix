# Remove the Mockos Host

## Change

Removed the unused `mockos` Darwin host configuration from the flake and
continuous integration matrix. Deleted its host module.

## Verification

The remaining Darwin hosts are `TP95V9LWWL` and `C02ZV797MD6R`. Run the Nix
flake check and pre-commit hooks before delivery.
