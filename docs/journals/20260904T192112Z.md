# Restrict the Darwin Container Package to ARM Hosts

## Date

2026-09-04T19:21:12Z

## Change

Made the `container` package available in `hosts/darwin/default.nix` only
when the host platform is `aarch64-darwin`.

## Verification

Run `nix flake check` and `pre-commit run --all-files`.
