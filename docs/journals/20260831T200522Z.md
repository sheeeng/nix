# Nono Test Override

Date: 2026-08-31T20:05:22Z

Added a Nixpkgs overlay that skips only
`why_self_reports_active_profile_deny_before_covering_allow` in `nono`.
The test conflicts with the Nix build sandbox because the nested sandbox
rejects a `/nix` grant that contains its protected state directory.
