2026-08-28: Terraform Overlay — Final Fixes

## Summary

Completed fixes across two repositories: `sheeeng/nix` and
All changes address the same root bug and its
downstream consequences.

## sheeeng/nix

### PR #229 — Merged

Three review comments addressed before merge:

1. **`fix(flake): rename overlayedPkgs`** — corrected past tense to
   "overlaid" throughout `flake.nix`.
2. **`fix(shell): apply overlay for nix-shell`** — expanded the
   default `pkgs` argument in `shell.nix` to set
   `config.allowUnfree = true` and inline the Terraform overlay,
   so legacy `nix-shell` users are not rejected.
3. Invalid download URLs in `overlays/default.nix` — already
   resolved in a prior session via `platformMap` nesting.

PR #229 merged to `unstable`. Workflow
`update-terraform-binary-version` triggered manually and opened
PR #230.

### PR #230 — Generated by workflow

Verified all four `1.16.0` hashes against HashiCorp directly:

| System | Hash |
| --- | --- |
| `aarch64-darwin` | `sha256-5HPwCmRdIusB9RDJa83qsqX9yObl4Mk1FfTa2xmvFXw=` |
| `aarch64-linux` | `sha256-02Zhr59Qb4XKSdPUdo+yZXDKj0wa1mCdXy35JJB9ix4=` |
| `x86_64-darwin` | `sha256-ouGntZN01Pro9tzu168Uz58SKs2eWziZZNtCEWStxXo=` |
| `x86_64-linux` | `sha256-moAwCFJSNfNuYDMAQKat3sPXjf59Nm8Sphj64Tt2D5w=` |

All correct. PR #230 ready to merge.

### flake-utils refactor

Replaced the custom `eachSystem` helper, `overlaidPkgs`, and
`nixpkgs.lib.genAttrs (import nix-systems)` calls with
`flake-utils.lib.eachDefaultSystem`. Non-system outputs
(`nixosConfigurations`, `darwinConfigurations`) merged via `//`.

## PR #4790

Same sed bug present. Three commits pushed:

1. **`fix(pkgs): protect platform map from sed`** — renamed
   `terraformTargets` to `platformMap` inside a nested `let` block
   (8-space indent after `nixfmt`), outside the workflow sed pattern
   (`^    "`). Verified: one 4-space match (`terraformHashes`), one
   8-space match (`platformMap`).
2. **`fix(treefmt): use pkgs.terraform from overlay`** — removed
   direct `import ./pkgs/terraform.nix` let binding from
   `treefmt.nix`; replaced with `${pkgs.terraform}/bin/terraform`.
3. PR description updated to document both fixes.

## Key Invariant

The `update-terraform-binary-version` workflow sed pattern is:

```text
```

Any attrset key at exactly four leading spaces with a system name
is rewritten with a hash. Platform strings must live at eight or
more spaces (nested `let`) to survive workflow runs.
